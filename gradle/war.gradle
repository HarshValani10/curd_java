apply plugin: "war"

bootWar {
    mainClass = "com.mycompany.myapp.Pro6App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
